services:
  api:
    build:
      context: .
      dockerfile: apps/api/Dockerfile
    environment:
      - NODE_ENV=staging
      - PORT=4000
    command: ["pnpm", "--filter", "@metascope/api", "run", "start:staging"]

  web:
    build:
      context: .
      dockerfile: apps/web/Dockerfile
    environment:
      - NODE_ENV=staging
      - PORT=3000
    command: ["pnpm", "--filter", "@metascope/web", "run", "start:staging"]
    depends_on:
      - api

  cms:
    build:
      context: .
      dockerfile: apps/cms/Dockerfile
    environment:
      - NODE_ENV=staging
      - PORT=3001
    command: ["pnpm", "--filter", "@metascope/cms", "run", "start:staging"]

  worker:
    build:
      context: .
      dockerfile: apps/worker/Dockerfile
    environment:
      - NODE_ENV=staging
    command: ["pnpm", "--filter", "@metascope/worker", "run", "start:staging"]
