#!/usr/bin/env sh

branch="$(git rev-parse --abbrev-ref HEAD)"
if [ "$branch" = "main" ]; then
  echo "⛔ Blocked: direct push to main is not allowed."
  echo "Please create/use a feature branch and open a PR."
  exit 1
fi

pnpm run check
